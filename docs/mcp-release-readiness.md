# MCP の結合検証・受入れ (#216 / #220)

**受入れ: 2026-09-15、依頼者の判断で #216 / #220 を受入れ済みとする。**
未検証項目は [#228](https://github.com/Shirataki2/DisCalendar/issues/228) と
[#229](https://github.com/Shirataki2/DisCalendar/issues/229) へ移管した。受入れは未検証項目の合格や本番への反映を意味しない。
利用ガイド・更新履歴は本番リリース時の利用者向けに整理し、一般公開・本番有効化は別途の指示で行う。

利用者向け手順は [利用ガイド](../web/src/content/docs/mcp.mdx)、実装契約は
[mcp-auth.md](mcp-auth.md) / [mcp-read.md](mcp-read.md) / [mcp-write.md](mcp-write.md)。

## 検証対象と記録方針

依頼者によるステージング検証。対象commit: `7c89c4b93b8bcbf6ffffcd3ac97547e60211ab8b`。
最初の8項目は2026-09-14 22:00 JST、追加確認は同日23:20 JST。

| 対象 | 版・方式 | 結果 |
| --- | --- | --- |
| Codex CLI | 0.154.0、CIMD | 認証・通常操作・下表の確認範囲は検証済み・期待値どおり |
| Codexデスクトップ | 26.908.40834、CIMD | 認証・通常操作・下表の確認範囲は検証済み・期待値どおり |
| MCP仕様版 | 交渉の確認は済み、実際の版の値は未記録 | 値の記録を #229 へ移管。SDKテスト値から推定しない |
| SDK / OAuth | @modelcontextprotocol/server 2.0.0、Better Auth/MCP/CIMD 1.7.4 | CIMD profile mcp-2026-07-28、DCR無効 |

個人開発の運用として、Git・PRには検証済みか、期待値どおりかの結果と対象版・commit・日時を記録する。
ログ・スクリーンショット・通信記録などの証跡や保存先、認証情報・認可URLのquery・予定本文は残さない。
未検証項目の移管による受入れと、実機検証済みは区別する。他クライアントへの対応保証はしない。

## 検証済みの範囲

以下は依頼者の確認結果であり、エージェントが実環境で再実行した結果ではない。

| 確認項目 | 結果 |
| --- | --- |
| 作成がWebに1件だけ表示され、ID・URLが返る | 検証済み・期待値どおり |
| タイトルのみの変更で説明・通知を保持 | 検証済み・期待値どおり |
| Web変更後の古いversionによる更新・削除を拒否 | 検証済み・期待値どおり |
| 同じキー・同じ入力の再送で重複せず元の結果を返す | 検証済み・期待値どおり |
| Discord連携時のDB/Discordの別結果と、最新versionでの削除 | 検証済み・期待値どおり |
| 15分経過後の更新トークンによる読み取り継続 | 検証済み・期待値どおり |
| 解除後の操作・更新拒否、Webログイン維持、更新期限切れ | 検証済み・期待値どおり |
| metadata・PKCE・callback・Discordログイン・同意と仕様交渉 | 検証済み・期待値どおり（仕様版の値のみ未記録） |
| 接続時の権限選択、サービス側の都度確認なし | 検証済み・期待値どおり |
| 読み取りscopeでの書き込み拒否、未許可サーバーの参照拒否 | 検証済み・期待値どおり |
| 編集ロール取消・本人退出・Bot退出・Discord権限取消・権限取消後の再送 | 検証済み・期待値どおり |
| HTTPS/Tunnel・challenge・Host/Origin・フォーム・CORS | 検証済み・期待値どおり |
| OAuth 16 KiB / MCP 64 KiBの入力上限（通常/chunked） | 検証済み・期待値どおり |
| CDN・Service Workerのキャッシュ除外 | 検証済み・期待値どおり |
| 通常のログイン・接続更新・予定操作 | 検証済み・期待値どおり（レート制限の適用・429は下記へ移管） |
| 新規停止・全停止・全失効・Webと接続解除の維持 | 検証済み・期待値どおり |
| introspection secret・JWT緊急交換・通常ローテーション | 検証済み・期待値どおり |
| DB復元・migration rollback・Webと接続解除の維持 | 検証済み・期待値どおり |
| web/api/cloudflared/Postgres/Alloy/Loki/Sentryのログ除外 | 検証済み・期待値どおり |
| 監査・管理者・Loki/Sentry・バックアップの保持設定と閲覧者 | 検証済み・期待値どおり（期限削除・墓標の実測は下記へ移管） |

## 未検証項目の引き継ぎ

Discord障害の再現には専用環境の準備が必要なため、通常機能の受入れから切り出す。
手元の確認表に残ったその他の未完了項目も、依頼者の明示的な判断で後続へ移す。

| 未完了項目 | 状態・行き先 |
| --- | --- |
| Discordの確定拒否・送信後の応答喪失・部分成功/unknownの実機案内 | 未検証 → #228 |
| 障害後の同じキーでの照会/再送・重複防止・変更/削除停止 | 未検証 → #228 |
| 24時間後の取得/再送拒否・本文消去・墓標維持・Discord/DB照合と復旧 | 未検証 → #228（墓標は #229 と結果を共有） |
| 実配信経路からの内部MCP認可・通常/管理API拒否・本人偽装拒否 | 未検証 → #229（既存自動テストとは別） |
| 同じキー・異なる入力を実Codexで拒否 | 未検証 → #229 |
| OAuth/MCP/エッジのレート制限適用・429・復帰 | 準備待ち → #229、共通エッジルールは #153 |
| 監査の期限削除・再実行防止の墓標の保持確認 | 未検証 → #229、墓標は #228 の結果参照可 |
| 実Codexの交渉MCP仕様版の値 | 未記録 → #229 |

MCPアプリのリクエスト頻度制限は未実装であり、書き込みロックを代替としない。
公開前の制限適用と残る運用判断は #229 で追跡する。#216 / #220 の受入れだけで設定済みとは扱わない。

## 配信・運用の契約

- ステージングは本番と別のorigin/resource/issuer/DB/Discordアプリ/Botを使う。
- web/apiのMCP_ENABLED、共通のMCP_INTROSPECTION_SECRET、webのBETTER_AUTH_URL、apiのMCP_AUTH_ORIGIN、API_URLを整合させる。
- HostはBETTER_AUTH_URLのhost（port込み）を維持する。転送ヘッダーで判定を上書きせず、Origin付き通信は同一originのみ許可する。
- Codexの非ブラウザ通信ではOrigin省略可。同意・解除フォームのOrigin省略/null/別originは拒否する。外部originのブラウザ呼び出しは許可しない。
- `/mcp` と配下、`/.well-known/`、`/api/auth/`、`/local/api/mcp/` はCDN・Service Workerのキャッシュ対象外とする。
- OAuthの入力上限16 KiB、MCPは64 KiB。通常/chunkedとも超過を拒否する。
- #153は認証/iCalの共通エッジ制限、#229はMCPを含む制限と実測を担当する。閾値は実トラフィックを根拠に決める。
- ログは発生元で機微情報を除外する。現行Alloyのテキスト転送を含め、監査ログだけでなく収集先とバックアップも確認対象にする。

## 停止・失効・鍵交換

設定変更後は該当する全web/apiプロセスを再起動し、実経路で確認する。停止前に実行中の操作を確認する。

compose運用では、ホストのルート `.env` に `MCP_NEW_CONNECTIONS_ENABLED=false` を設定し、
`docker compose up -d --no-deps web` でwebコンテナへ反映する。`docker compose restart` だけでは環境変数は更新されない。
標準デプロイの `.github/scripts/deploy.sh` も `docker compose up -d` を使うため、この設定を反映する。
確認は `docker compose exec web printenv MCP_NEW_CONNECTIONS_ENABLED` でこのフラグだけを表示する。
再開時は `true` に戻して同じ手順を実施する。MCP本体の有効化と専用DBの準備は別途必要。

| 操作 | 設定/手段 | 維持するもの |
| --- | --- | --- |
| 新規接続停止 | webのMCP_NEW_CONNECTIONS_ENABLED=false（既定は受付）。認可・ログイン開始・同意・認可コード交換を停止 | 既存アクセストークン、更新トークン、Webログイン、接続解除 |
| MCP/OAuth全停止 | web/apiのMCP_ENABLED=false | 通常Webログイン、/mcp/connectionsと解除 |
| 個別解除 | 利用者の接続管理から解除 | 別接続とWebセッション |
| 全接続失効 | 全停止して下記SQLを管理DB接続で実行 | Webのuser/account/session |

全接続失効は対象環境のバックアップとDB名を確認し、運用管理者が実施する。

```sql
BEGIN;
UPDATE mcp_connections SET revoked_at = COALESCE(revoked_at, now());
DELETE FROM "oauthRefreshToken";
DELETE FROM "oauthConsent";
COMMIT;
```

停止だけでは失効しない。侵害対応では必ず失効も行う。再開前に古いトークンの拒否と新規接続を確認する。
MCP専用introspection secretの交換は全停止→web/api双方へ同じ新しい32文字以上のランダム値を設定→全プロセス再起動→疎通→再開。
秘密値はコマンド引数・ログ・PRへ書かない。

JWT署名鍵を緊急交換する場合は全停止・全接続失効後、管理DBでjwksを削除し、web全プロセスを再起動する。
固定版JWTプラグインによる新しい鍵生成とJWKSのkid変更、古いJWTの拒否を検証してから再開する。
BETTER_AUTH_SECRETの変更はWebセッションにも影響するため、MCP専用鍵交換の代用にはしない。
通常の鍵ローテーションを含め、依頼者が専用環境で検証済み・期待値どおりと確認した。

## 障害・DB復旧・ロールバック

1. MCPを全停止し、新しい書き込みを止める。結果不明の操作を抽出し、[照合手順](mcp-write.md#結果不明からの復旧)でDiscordとDBを確定する。
2. DBバックアップと対象イメージ、適用migration一覧を確保する。全接続を失効させる。未解決状態のまま操作テーブルを削除しない。
3. [運用手順](operations.md) に従いapi/botを停止する。戻す版にないmigrationがある場合は、イメージを戻す前にDBを戻す。
4. MCP操作テーブルを戻す必要がある場合のみ api/rollback/20260913090000_mcp_event_operations.sql を実行する。
   SQLxの履歴処理は運用手順に従い、適用済みmigrationを編集しない。その後APIは全プロセスを同じ旧版にする。Webの接続解除を提供する版を維持する。
5. OAuth DDLを撤去する web/migrations/mcp/rollback.sql は検証DBの廃棄用。接続管理もテーブルを必要とするため、停止中の解除を維持する運用では実行しない。
6. DBをバックアップから復元すると接続の失効・操作記録も過去に戻る。MCPを停止したまま再度全失効し、復元時点以後のDiscord副作用を照合する。
   古いクライアントからの再送を許可しない。照合後に新規同意と新しい操作で再開する。

## 監査と親Issueの受入れ

監査はadmin_audit_logsに30日、結果本文は24時間でアクセス不可・毎時消去で最大約25時間、再実行防止の墓標は無期限という契約。
監査画面の閲覧者は既存の管理者認証を通る利用者。Loki/Sentry・バックアップの保持設定と閲覧者は依頼者が確認済み。
期限削除と墓標の実際の動作確認は #228 / #229 に残す。

| #216 の設計条件 | 受入れ時の整理 |
| --- | --- |
| 公開範囲・クライアント版・仕様 | Codex CLI/デスクトップを対象。版は上表、交渉仕様版の値は #229 |
| URL→Discord→同意→PKCE→読み取り | 依頼者の実機確認済み |
| Rust認証・scope・guild・管理API拒否 | #218の実装・自動テストと実機のscope/guild確認済み。内部経路の残確認は #229 |
| 同意・解除・期限・権限反映・操作確認 | mcp-auth/read/write.md、依頼者の実機確認済み |
| 入出力・日時・競合・再送・Discord副作用 | #218/#219と実装docs。通常系確認済み、異なる入力の再送は #229、障害時は #228 |
| 異常系の検証項目 | 認証・scope・権限・期限・競合は確認済み。未検証を #228/#229 へ移管 |
| DB・rollback・レート制限・監査・公開経路 | 停止/復旧・配信・ログは確認済み。制限と期限削除等は #229 |
| 設計記録と実装分割 | #217→#218→#219→#220、確定した実装契約はmcp-auth/read/write.mdと本書。#221は初期設計の参照原稿 |

#220の利用ガイド・更新履歴は #227 で本番利用者向けに整理する。
#216 / #220 の受入れ後も #228 / #229 の未検証状態を維持し、後続結果は本書へ反映する。
